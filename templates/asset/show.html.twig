{% extends 'base.html.twig' %}

{% block title %}Asset{% endblock %}

{% block body %}
    <h1>Asset</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asset.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ asset.label }}</td>
            </tr>
            <tr>
                <th>Currency</th>
                <td>{{ asset.currency.value }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ asset.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('asset_index') }}">back to list</a>

    <a href="{{ path('asset_edit', {'id': asset.id}) }}">edit</a>

    {{ include('asset/_delete_form.html.twig') }}
{% endblock %}
